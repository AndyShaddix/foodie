import 'package:flutter/material.dart';

class ExplorarTab extends StatelessWidget {
  const ExplorarTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Container(child: Text("Menu")));
  }
}
